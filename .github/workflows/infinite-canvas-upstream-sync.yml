name: Sync Infinite Canvas Upstream

on:
  workflow_dispatch:
  schedule:
    - cron: '7 * * * *'

concurrency:
  group: infinite-canvas-upstream-sync
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  discover:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      changed: ${{ steps.upstream.outputs.changed }}
      repository_sha: ${{ steps.repository.outputs.sha }}
      current_sha: ${{ steps.upstream.outputs.current_sha }}
      latest_sha: ${{ steps.upstream.outputs.latest_sha }}
      release_tag: ${{ steps.upstream.outputs.release_tag }}
    env:
      CANVAS_REPOSITORY: basketikun/infinite-canvas
    steps:
      - name: Checkout trusted repository descriptor
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: main
          fetch-depth: 0
          submodules: recursive
          persist-credentials: false

      - name: Capture immutable repository source
        id: repository
        shell: bash
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Read Infinite Canvas published release
        id: canvas_release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          INFINITE_CANVAS_RELEASE_FILE=/tmp/infinite-canvas-release.txt
          INFINITE_CANVAS_RELEASE_ERROR_FILE=/tmp/infinite-canvas-release-error.log
          INFINITE_CANVAS_RELEASE_TAG=""
          if gh api "repos/${CANVAS_REPOSITORY}/releases/latest" --jq '.tag_name // ""' > "$INFINITE_CANVAS_RELEASE_FILE" 2> "$INFINITE_CANVAS_RELEASE_ERROR_FILE"; then
            INFINITE_CANVAS_RELEASE_TAG="$(tr -d '[:space:]' < "$INFINITE_CANVAS_RELEASE_FILE")"
          elif grep -Eiq '404|not found' "$INFINITE_CANVAS_RELEASE_ERROR_FILE"; then
            echo "No published Infinite Canvas release; falling back to main."
          else
            cat "$INFINITE_CANVAS_RELEASE_ERROR_FILE" >&2
            exit 1
          fi
          if [[ -n "$INFINITE_CANVAS_RELEASE_TAG" ]] && ! git check-ref-format "refs/tags/${INFINITE_CANVAS_RELEASE_TAG}"; then
            echo "Invalid Infinite Canvas release tag: $INFINITE_CANVAS_RELEASE_TAG" >&2
            exit 1
          fi
          printf 'tag=%s\n' "$INFINITE_CANVAS_RELEASE_TAG" >> "$GITHUB_OUTPUT"
          echo "Infinite Canvas published release: ${INFINITE_CANVAS_RELEASE_TAG:-none}"

      - name: Detect immutable upstream update
        id: upstream
        env:
          RELEASE_TAG: ${{ steps.canvas_release.outputs.tag }}
        shell: bash
        run: |
          set -euo pipefail
          CURRENT_SHA="$(git rev-parse HEAD:integrations/infinite-canvas)"
          if [[ -n "$RELEASE_TAG" ]]; then
            git -C integrations/infinite-canvas fetch --depth=1 origin "refs/tags/${RELEASE_TAG}:refs/tags/${RELEASE_TAG}"
            LATEST_SHA="$(git -C integrations/infinite-canvas rev-list -n 1 "refs/tags/${RELEASE_TAG}")"
          else
            git -C integrations/infinite-canvas fetch --depth=1 origin main
            LATEST_SHA="$(git -C integrations/infinite-canvas rev-parse FETCH_HEAD)"
          fi
          echo "current_sha=$CURRENT_SHA" >> "$GITHUB_OUTPUT"
          echo "latest_sha=$LATEST_SHA" >> "$GITHUB_OUTPUT"
          echo "release_tag=$RELEASE_TAG" >> "$GITHUB_OUTPUT"
          if [[ "$CURRENT_SHA" == "$LATEST_SHA" ]]; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            echo "Infinite Canvas upstream is unchanged."
          else
            git -C integrations/infinite-canvas fetch --depth=1000 origin main
            if ! git -C integrations/infinite-canvas merge-base --is-ancestor "$CURRENT_SHA" "$LATEST_SHA"; then
              echo "Refusing to downgrade or cross-grade Infinite Canvas: $CURRENT_SHA -> $LATEST_SHA" >&2
              exit 1
            fi
            echo "changed=true" >> "$GITHUB_OUTPUT"
            echo "Infinite Canvas update: $CURRENT_SHA -> $LATEST_SHA"
          fi

  validate-update:
    needs: discover
    if: needs.discover.outputs.changed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      LATEST_SHA: ${{ needs.discover.outputs.latest_sha }}
      RELEASE_TAG: ${{ needs.discover.outputs.release_tag }}
    steps:
      - name: Checkout immutable trusted repository source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ needs.discover.outputs.repository_sha }}
          fetch-depth: 0
          submodules: recursive
          persist-credentials: false

      - name: Materialize discovered Canvas commit
        shell: bash
        run: |
          set -euo pipefail
          test -n "$LATEST_SHA"
          if [[ -n "$RELEASE_TAG" ]]; then
            git -C integrations/infinite-canvas fetch --depth=1 origin "refs/tags/${RELEASE_TAG}:refs/tags/${RELEASE_TAG}"
          else
            git -C integrations/infinite-canvas fetch --depth=1 origin main
          fi
          git -C integrations/infinite-canvas fetch --depth=1000 origin main
          git -C integrations/infinite-canvas checkout --detach "$LATEST_SHA"
          [[ "$(git -C integrations/infinite-canvas rev-parse HEAD)" == "$LATEST_SHA" ]]

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2
        with:
          bun-version: 1.3.13

      - name: Verify adapter against updated upstream
        shell: bash
        run: |
          set -euo pipefail
          node scripts/apply-infinite-canvas-patches.mjs --root integrations/infinite-canvas
          pushd integrations/infinite-canvas/web
          bun install --frozen-lockfile
          VITE_BASE=/canvas-app/ bun run typecheck
          VITE_BASE=/canvas-app/ bun run build
          cp ../VERSION dist/version.txt
          popd
          git -C integrations/infinite-canvas reset --hard "$LATEST_SHA"
          git -C integrations/infinite-canvas clean -f -- web/src/lib/sub2-bridge.ts
          test -z "$(git -C integrations/infinite-canvas status --short)"

      - name: Run integration contract tests
        shell: bash
        run: |
          set -euo pipefail
          node scripts/apply-sub2-infinite-canvas-integration.mjs --root . --check
          node --test scripts/__tests__/infinite-canvas-integration.test.mjs scripts/__tests__/infinite-canvas-workflows.test.mjs

  publish-update:
    needs: [discover, validate-update]
    if: needs.discover.outputs.changed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    outputs:
      update_sha: ${{ steps.update_branch.outputs.update_sha }}
      pr_number: ${{ steps.pull_request.outputs.pr_number }}
    env:
      UPDATE_BRANCH: automation/infinite-canvas-upstream
      LATEST_SHA: ${{ needs.discover.outputs.latest_sha }}
      RELEASE_TAG: ${{ needs.discover.outputs.release_tag }}
    steps:
      - name: Checkout the exact validated repository source
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ needs.discover.outputs.repository_sha }}
          fetch-depth: 0
          submodules: recursive
          persist-credentials: false

      - name: Materialize validated submodule pointer
        shell: bash
        run: |
          set -euo pipefail
          test -n "$LATEST_SHA"
          if [[ -n "$RELEASE_TAG" ]]; then
            git -C integrations/infinite-canvas fetch --depth=1 origin "refs/tags/${RELEASE_TAG}:refs/tags/${RELEASE_TAG}"
          else
            git -C integrations/infinite-canvas fetch --depth=1 origin main
          fi
          git -C integrations/infinite-canvas fetch --depth=1000 origin main
          git -C integrations/infinite-canvas checkout --detach "$LATEST_SHA"
          [[ "$(git -C integrations/infinite-canvas rev-parse HEAD)" == "$LATEST_SHA" ]]
          test "$(git diff --name-only)" = "integrations/infinite-canvas"

      - name: Create update branch
        id: update_branch
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git checkout -B "$UPDATE_BRANCH"
          git add integrations/infinite-canvas
          git commit -m "chore: update Infinite Canvas upstream to $LATEST_SHA"
          UPDATE_SHA="$(git rev-parse HEAD)"
          gh auth setup-git
          git push --force origin "$UPDATE_BRANCH"
          echo "update_sha=$UPDATE_SHA" >> "$GITHUB_OUTPUT"

      - name: Create or update pull request
        id: pull_request
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          PR_NUMBER="$(gh pr list --repo "$GITHUB_REPOSITORY" --head "$UPDATE_BRANCH" --base main --state open --json number --jq '.[0].number // empty')"
          if [[ -z "$PR_NUMBER" ]]; then
            gh pr create \
              --repo "$GITHUB_REPOSITORY" \
              --base main \
              --head "$UPDATE_BRANCH" \
              --title "chore: 自动同步 Infinite Canvas 上游" \
              --body "自动更新 Infinite Canvas 子模块。适配补丁、TypeScript 检查和生产构建已通过；合并仍受仓库完整 CI 门禁保护。"
            PR_NUMBER="$(gh pr list --repo "$GITHUB_REPOSITORY" --head "$UPDATE_BRANCH" --base main --state open --json number --jq '.[0].number')"
          fi
          test -n "$PR_NUMBER"
          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"

  full-ci:
    needs: [discover, publish-update]
    if: needs.discover.outputs.changed == 'true'
    permissions:
      contents: read
    uses: ./.github/workflows/backend-ci.yml
    with:
      ref: ${{ needs.publish-update.outputs.update_sha }}

  merge:
    needs: [discover, publish-update, full-ci]
    if: needs.discover.outputs.changed == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Merge verified Infinite Canvas update
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ needs.publish-update.outputs.pr_number }}
          UPDATE_SHA: ${{ needs.publish-update.outputs.update_sha }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$PR_NUMBER"
          test -n "$UPDATE_SHA"
          PR_HEAD_SHA="$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json headRefOid --jq '.headRefOid')"
          if [[ "$PR_HEAD_SHA" != "$UPDATE_SHA" ]]; then
            echo "Infinite Canvas PR head changed after CI: $PR_HEAD_SHA != $UPDATE_SHA" >&2
            exit 1
          fi
          [[ "$PR_HEAD_SHA" == "$UPDATE_SHA" ]]
          gh pr merge "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --match-head-commit "$UPDATE_SHA" --squash
          PR_STATE="$(gh pr view "$PR_NUMBER" --repo "$GITHUB_REPOSITORY" --json state --jq '.state')"
          if [[ "$PR_STATE" != "MERGED" ]]; then
            echo "Infinite Canvas update pull request was not merged: $PR_STATE" >&2
            exit 1
          fi

  dispatch-release:
    needs: [discover, publish-update, full-ci, merge]
    if: ${{ always() && needs.discover.result == 'success' && (needs.discover.outputs.changed == 'false' || (needs.discover.outputs.changed == 'true' && needs.publish-update.result == 'success' && needs.full-ci.result == 'success' && needs.merge.result == 'success')) }}
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: Trigger unified upstream release sync
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          gh workflow run upstream-theme-sync.yml \
            --repo "$GITHUB_REPOSITORY" \
            --ref main \
            -f repository_release=false \
            -f scheduled_round=true
